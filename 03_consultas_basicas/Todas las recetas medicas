-- Ver todas las recetas con doctor y paciente

SELECT 
    id_receta,
    numero_receta,
    fecha_emision,
    medico,
    paciente,
    indicaciones
FROM recetas_medicas
ORDER BY fecha_emision DESC;
